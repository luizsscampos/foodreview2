<% if flash.any? %>
    <% flash.each do |key, message | %>
        <div class="text-center alert alert-<%= key %>">
            <%= message %>
        </div>
        <p class="notice"><%= notice %></p>
    <% end %>
<% end %>
